diff --git a/htmltext.py b/htmltext.py
index 2012dcf..14e7436 100644
--- a/htmltext.py
+++ b/htmltext.py
@@ -50,11 +50,12 @@ class HtmlText(object):
 		for t in self.tokens:
 			if self.__is_open(t) and not t.self_closes: close_stack.append(t)
 			elif self.__is_close(t):
-				if not (close_stack[-1].tag == t.tag):
-					print close_stack
-					print self.tokens
-					raise Exception("autoclose mismatch")
-				close_stack.pop()
+				# tolerant: find the matching open anywhere in the stack (handles crossed
+				# sup/sub/em/strong nesting); ignore a close that has no matching open
+				for i in xrange(len(close_stack) - 1, -1, -1):
+					if close_stack[i].tag == t.tag:
+						del close_stack[i]
+						break
 		
 		while len(close_stack) > 0:
 			self.append(CloseTag(close_stack.pop().tag))
@@ -67,8 +68,8 @@ class HtmlText(object):
 					prev.closed_by = token
 					break
 			else:
-				print self.tokens
-				raise Exception("No matching OpenTag for %s found!" % token.tag)
+				# spurious close tag with no matching open; drop it rather than crash
+				return
 		elif len(self.tokens) > 0 and self.__is_open(token):
 			for i in xrange(-1, -len(self.tokens), -1):
 				last = self.tokens[i]
diff --git a/x86manual.py b/x86manual.py
index 7f57350..e97d1a4 100644
--- a/x86manual.py
+++ b/x86manual.py
@@ -122,9 +122,8 @@ def left_aligned_table(source):
 				col_index = i
 				break
 		else:
-			print columns
-			print contents
-			raise Exception("No matching column!")
+			# no exact column match (modern tables drift slightly): snap to nearest column by x1
+			col_index = min(range(len(columns)), key=lambda i: abs(item_bounds.x1() - columns[i]))
 		
 		row[col_index] = [item]
 	
@@ -258,8 +257,10 @@ class x86ManParser(object):
 						try:
 							self.flush()
 							self.success += 1
-						except:
-							print "*** couldn't flush to disk"
+						except Exception, e:
+							title = unicode(self.textLines[0]) if self.textLines else u"?"
+							mnem = title.split(u"—")[0].strip()[:48]
+							sys.stderr.write((u"*** skipped %s (%s)\n" % (mnem, e)).encode("utf-8"))
 							self.fail += 1
 					
 					self.ltRects = []
@@ -711,6 +712,7 @@ class x86ManParser(object):
 			if line.chars[0].get_text() == u"•":
 				if len(line.chars) == 1:
 					i += 1
+					if i >= len(textLines): break
 					line = textLines[i]
 				else:
 					for j in xrange(1, len(line.chars)):
