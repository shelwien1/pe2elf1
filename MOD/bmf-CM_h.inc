

static const int CM_max_neighbours = (24+0) * (1);
static const int CM_orders = (4+0) * (1);
static const int CM_max_alpha = (16+0) * (1);
static const int CM_match_lens = (32+0) * (1);
static const int CM_count_cap_max = (255+0) * (1);
static const int CM_short_nb = (4+0) * (1);
static const int CM_table_bits = (20+0) * (1);
static const int CM_mid_bits = (18+0) * (1);
static const int CM_match_bits = (20+0) * (1);
static const int CM_nb_for_bits_1 = (24+0) * (1);
static const int CM_nb_for_bits_2 = (10+0) * (1);
static const int CM_nb_for_bits_3 = (7+0) * (1);
static const int CM_nb_for_bits_4 = (7+0) * (1);
static const int CM_count_cap = (60+0) * (1);
static const int CM_adapt_offset = (2+0) * (1);
static const int CM_blend = (24+0) * (1);
static const int CM_nbucket_shift = (3+0) * (1);
static const int CM_match_span = (12+0) * (1);
static const int CM_match_cap = (40+0) * (1);
static const int CM_match_seed = (40000+0) * (1);
static const int CM_probe_shift = (17+0) * (1);
static const int CM_probe_bands = (8+0) * (1);
static const int CM_probe_num_1 = (5+0) * (1);
static const int CM_probe_den_1 = (6+0) * (1);
static const int CM_probe_round_1 = (3+0) * (1);
static const int CM_probe_num_2 = (2+0) * (1);
static const int CM_probe_den_2 = (3+0) * (1);
static const int CM_probe_round_2 = (1+0) * (1);
static const int CM_probe_num_3 = (1+0) * (1);
static const int CM_probe_den_3 = (2+0) * (1);
static const int CM_probe_round_3 = (1+0) * (1);
static const int CM_probe_bias = (6+0) * (1);
static const int CM_key_hash = (2654435761+0) * (1);
static const int CM_cell_seed = (32768+0) * (1);
static const int CM_dx_00 = (1+0) * (-1);
static const int CM_dy_00 = (0+0) * (1);
static const int CM_dx_01 = (0+0) * (1);
static const int CM_dy_01 = (1+0) * (-1);
static const int CM_dx_02 = (1+0) * (-1);
static const int CM_dy_02 = (1+0) * (-1);
static const int CM_dx_03 = (1+0) * (1);
static const int CM_dy_03 = (1+0) * (-1);
static const int CM_dx_04 = (2+0) * (-1);
static const int CM_dy_04 = (0+0) * (1);
static const int CM_dx_05 = (0+0) * (1);
static const int CM_dy_05 = (2+0) * (-1);
static const int CM_dx_06 = (2+0) * (1);
static const int CM_dy_06 = (1+0) * (-1);
static const int CM_dx_07 = (2+0) * (-1);
static const int CM_dy_07 = (1+0) * (-1);
static const int CM_dx_08 = (1+0) * (-1);
static const int CM_dy_08 = (2+0) * (-1);
static const int CM_dx_09 = (1+0) * (1);
static const int CM_dy_09 = (2+0) * (-1);
static const int CM_dx_10 = (3+0) * (-1);
static const int CM_dy_10 = (0+0) * (1);
static const int CM_dx_11 = (0+0) * (1);
static const int CM_dy_11 = (3+0) * (-1);
static const int CM_dx_12 = (2+0) * (1);
static const int CM_dy_12 = (2+0) * (-1);
static const int CM_dx_13 = (2+0) * (-1);
static const int CM_dy_13 = (2+0) * (-1);
static const int CM_dx_14 = (3+0) * (1);
static const int CM_dy_14 = (1+0) * (-1);
static const int CM_dx_15 = (3+0) * (-1);
static const int CM_dy_15 = (1+0) * (-1);
static const int CM_dx_16 = (4+0) * (-1);
static const int CM_dy_16 = (0+0) * (1);
static const int CM_dx_17 = (0+0) * (1);
static const int CM_dy_17 = (4+0) * (-1);
static const int CM_dx_18 = (1+0) * (1);
static const int CM_dy_18 = (3+0) * (-1);
static const int CM_dx_19 = (1+0) * (-1);
static const int CM_dy_19 = (3+0) * (-1);
static const int CM_dx_20 = (3+0) * (1);
static const int CM_dy_20 = (2+0) * (-1);
static const int CM_dx_21 = (3+0) * (-1);
static const int CM_dy_21 = (2+0) * (-1);
static const int CM_dx_22 = (4+0) * (1);
static const int CM_dy_22 = (1+0) * (-1);
static const int CM_dx_23 = (4+0) * (-1);
static const int CM_dy_23 = (1+0) * (-1);



struct CM_T { 
#undef USE_NEW
#define USE_NEW 0


  
  /* Total bytes this module's Table() members occupy -- a constant here, since
   * Const mode has already folded every count. */
  static constexpr unsigned long long CM_Size = 0;
  void CM_Init( void ) {
  
  }
  void CM_Quit( void ) {
  }
};
