

static const int GL_max_width = (65535+0) * (1);
static const int GL_no_symbol = (8192+0) * (1);
static const int GL_model_table_bytes = (520192+0) * (1);
static const int GL_bank_mirror_mask = (32752+0) * (1);
static const int GL_p2_row_pad = (234+0) * (1);
static const int GL_weight_min_neg = (64+0) * (1);
static const int GL_weight_max = (191+0) * (1);
static const int GL_p2_freq_rescale_total = (29696+0) * (1);
static const int GL_ctx_id3_limit = (53248+0) * (1);
static const int GL_slack_shift = (7+0) * (1);
static const int GL_slack_max = (16384+0) * (1);
static const int GL_seed_shift = (5+0) * (1);
static const int GL_rescale_force = (251+0) * (1);
static const int GL_rescale_bias_ratio = (20+0) * (1);
static const int GL_escape_floor = (256+0) * (1);
static const int GL_escape_kick = (15+0) * (1);
static const int GL_blend_share = (21+0) * (1);
static const int GL_blend_lvl_scale = (8+0) * (1);



struct GL_T { 
#undef USE_NEW
#define USE_NEW 0


  
  /* Total bytes this module's Table() members occupy -- a constant here, since
   * Const mode has already folded every count. */
  static constexpr unsigned long long GL_Size = 0;
  void GL_Init( void ) {
  
  }
  void GL_Quit( void ) {
  }
};
