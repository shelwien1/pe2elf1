

static const int P0_CT_wr = (eSCALE/(496+16));
static const int P0_CT_mw = (257+1) * (1);
static const int P0_CTF_wr = (eSCALE/(144+16));
static const int P0_CTF_mw = (41+1) * (1);
static const int P0_CTN_wr = (eSCALE/(1264+16));
static const int P0_EL_wr = (eSCALE/(368+16));
static const int P0_EL_mw = (457+1) * (1);
static const int P0_EP_wr = (eSCALE/(84+16));
static const int P0_EP_mw = (528+1) * (1);
static const int P0_BY_wr = (eSCALE/(304+16));
static const int P0_BY_mw = (19+1) * (1);
static const int P0_PR_wr = (eSCALE/(7+16));
static const int P0_PR_mw = (0+1) * (1);
static const int P0_RL_wr = (eSCALE/(240+16));
static const int P0_RL_mw = (23+1) * (1);
static const int P0_FL_wr = (eSCALE/(8+16));
static const int P0_FL_mw = (399+1) * (1);
static const int P0_MX_LR = (26+1) * (1);
static const int P0_MX_W0 = (11717+1) * (1);
static const int P0_AP_RATE = (7+1) * (1);
static const int P0_AP_WT = (7+1) * (1);
static const int P0_AP_WT2 = (9+1) * (1);
static const int P0_ACT_E0 = (341+1) * (1);
static const int P0_ACT_E1 = (1+1) * (1);
static const int P0_ACT_E2 = (3+1) * (1);
static const int P0_ACT_E3 = (7+1) * (1);
static const int P0_ACT_E4 = (13+1) * (1);
static const int P0_ACT_E5 = (34+1) * (1);
static const int P0_ACT_E6 = (102+1) * (1);
static const int P0_GRP_E0 = (132+1) * (1);
static const int P0_GRP_E1 = (2+1) * (1);
static const int P0_GRP_E2 = (5+1) * (1);
static const int P0_GRP_E3 = (9+1) * (1);
static const int P0_GRP_E4 = (15+1) * (1);
static const int P0_GRP_E5 = (26+1) * (1);
static const int P0_GRP_E6 = (51+1) * (1);
static const int P0_AW_W = (21+0) * (1);
static const int P0_AW_N = (4+0) * (1);
static const int P0_AW_NW = (0+0) * (1);
static const int P0_AW_NE = (8+0) * (1);
static const int P0_AW_NN = (2+0) * (1);
static const int P0_AW_WW = (3+0) * (1);
static const int P0_AW_X = (6+0) * (1);
static const int P0_DZ0 = (0+1) * (1);
static const int P0_DZ1 = (0+1) * (1);
static const int P0_DZ2 = (1+1) * (1);
static const int P0_DZ3 = (1+1) * (1);
static const int P0_DZ4 = (1+1) * (1);
static const int P0_DZ5 = (3+1) * (1);
static const int P0_DZ6 = (3+1) * (1);
static const int P0_DZ7 = (3+1) * (1);
static const int P0_MX_LR2 = (9+1) * (1);
static const int P0_LM_MU = (4+1) * (1);
static const int P0_LM_LIM = (3071+1) * (16);
static const int P0_CTS_wr = (eSCALE/(1528+16));



struct P0_T { 
#undef USE_NEW
#define USE_NEW 0


  
  /* Total bytes this module's Table() members occupy -- a constant here, since
   * Const mode has already folded every count. */
  static constexpr unsigned long long P0_Size = 0;
  void P0_Init( void ) {
  
  }
  void P0_Quit( void ) {
  }
};
