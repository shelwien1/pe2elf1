// Template for sh_model-B0.idx.  B0 declares parameters only -- no Index,
// no Table -- so there is nothing to place: with no MakeTables marker here,
// idx2inc.pl routes every declaration to sh_model-B0_h.inc, which is what
// bmpc.cpp includes.  The generated sh_model-B0_p.inc is empty by design.
