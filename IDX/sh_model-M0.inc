
// Context of the mixer, expanded by IDX/idx2inc.pl from the Index Cx block
// of sh_model-M0.idx (MOD/sh_model-M0_p.inc).
static uint M0_MakeCx( uint c2, uint c1, uint cxt ) {
  int Cx;
  MakeIndex Cx
  return uint(Cx);
}
