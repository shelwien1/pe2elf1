Linux x86-64, clang++-18, ./build.sh (-march=haswell), single thread.
"ppmd" = ./coder0 c <f> <o> none (transformer disabled), "lstm" = the original
coder0 this tree started from, "tf" = ppmd + the pretrained transformer.

file                 orig     ppmd     lstm       tf   tf vs ppmd   tf vs lstm
book1000             4096     1858     1821     1734       -6.7%        -4.8%
book1[:16384]       16384     6312     6104     6003       -4.9%        -1.7%
book1[:65536]       65536    21803    20796    21085       -3.3%        +1.4%
book1              768771   209801   194387   204669       -2.4%        +5.3%

All four round-trip byte-identically (./t.sh).

book1: enc 220s, dec 220s (~3500 bytes/s; ~110 quantized matmuls and a 2.9 MB
sequential weight stream per byte).  Peak RSS 435 MB, of which ~400 MB is the
ppmd arena (ppmd_memory = 6284) and ~37 MB the transformer.

The frozen model is strongest on short inputs and loses to the on-line LSTM
past ~30 KB: it was trained on WRT-dictionary-encoded Wikipedia, so plain
ASCII prose is out of its training distribution, while the LSTM adapts to the
file at hand.  It still beats ppmd alone everywhere.

Token mapping (book1000, same build):
  enwik9-derived byte map (what this tree does)   1734
  coder0 packed alphabet indices                  2140   worse than no model
  no transformer                                  1858
Cross-entropy of the transformer alone on book1[:20000] with a flat prior:
  enwik9-derived byte map    3.73 bits/byte
  packed alphabet indices    7.19 bits/byte
  scrambled map (control)    8.33 bits/byte

Switch matrix (clang++-18, -march=haswell), all round-tripping:

config                              book1000    16K      64K
pretrained, frozen (default)            1734   6003    21085
pretrained + TF_TRAIN=1                 1761   5977    20770
fresh init (TF_LOAD_WEIGHTS=0)          2042   6672    22356
fresh init + TF_TRAIN=1                 1880   6284    21565
  (for reference: ppmd alone            1858   6312    21803
                  ppmd + LSTM           1821   6104    20796)

Online learning rate on book1[:65536], pretrained weights:
  frozen 21085, lr 7200 -> 20956, 600 -> 20796, 1000 -> 20772, 2000 -> 20770.
The LSTM's 7200 is tuned for a model starting from noise; 2000 is the default
here, and is also the best of the values tried from a fresh init.

Online training (TF_TRAIN), pretrained weights, lr 2000:

  config              book1000    16K      64K     book1
  frozen                  1734   6003    21085    204669
  TF_TRAIN=1              1761   5977    20770    200299
  TF_TRAIN=2              1766   5966    20684    200026
  (ppmd alone             1858   6312    21803    209801)
  (ppmd + LSTM            1821   6104    20796    194387)

TF_ADAPTER_D=384 is not better than the default 192 (1777 / 6004 / 20687).

tools/gradcheck.cpp, directional derivative vs the analytic gradient:
  head      numeric -12.951033  analytic -12.951110  rel 3.0e-06
  mlp.down  numeric  +2.725932  analytic  +2.725839  rel 1.7e-05
  mlp.up    numeric  -1.893722  analytic  -1.893601  rel 3.2e-05

fp32 engine (TF_FP32=1, the default: weights dequantized at load, no activation
quantization anywhere) against the packed int4/int8 engine (TF_FP32=0):

  config                       book1000    16K      64K    time/4KB
  quantized engine                 1734   6003    21085       1.7s
  fp32 engine                      1735   6002    21086       6.9s
  quantized + TF_TRAIN=2           1766   5966    20684       2.0s
  fp32 + TF_TRAIN=1                1761   5976    20756       6.7s
  fp32 + TF_TRAIN=2                1765   5957    20666       6.6s
  fp32 + fresh init                2046   6680    22364       6.2s

Compression is the same within a byte or two - the model was trained with fake
quantization, so running it in full precision is at worst neutral.  tools/
engine_cmp.cpp on a synthetic stream: mean |dp| 1.0e-03 per element, and the
fp32 engine's mean -log p is LOWER by 0.0175 nats.

The fp32 model is 5,897,145 parameters = 22.50 MB, contiguous.  engine_cmp
also checks the backup contract: copy the block, scale every weight by 1.03
(output moves by 0.12 L1), copy the block back, and the output is bit-identical
again.

TF_TRAIN=3, the whole model trained online (fp32, AdamW per byte, lr 3e-6):

  config                    book1000    16K      64K     book1
  ppmd alone                    1858   6312    21803   209801
  ppmd + LSTM (original)        1821   6104    20796   194387
  transformer frozen            1735   6002    21086   204669
  TF_TRAIN=1 (output layer)     1761   5976    20756   200299
  TF_TRAIN=2 (top block)        1765   5957    20666   200026
  TF_TRAIN=3 (whole model)      1671   5700    20038   194251
                               -8.2%  -6.6%   -3.6%   -0.07%  vs the LSTM

book1 with TF_TRAIN=3: 6398s to encode, 6400s to decode, round-trip verified.
The frozen model lost book1 to the LSTM by 10,282 bytes; training the model
closes the whole gap.

Learning rate (book1000 / 16K, frozen 1735 / 6002):
  1e-6 1693/5751   3e-6 1671/5700   5e-6 1667/5706   1e-5 1669/5752
  3e-5 1707/5878   1e-4 1778/6034   3e-4 1810/6105
64K: 3e-6 -> 20038, 5e-6 -> 20100.  Longer inputs prefer the lower end.

Gradient accumulation (book1000 / 16K, time per 4 KB):
  TF_BATCH=1  1671 / 5700  36s
  TF_BATCH=4  1671 / 5744  22s
  TF_BATCH=16 1697 / 5828  18s

tools/gradcheck_full.cpp, numeric directional derivative vs the analytic
gradient, with the recurrent state restored between evaluations so the finite
difference matches the one-step truncation:
  whole model              -26.057428 vs -26.055626   rel 3.5e-05
  embedding+prior+unembed   -1.712434 vs  -1.712887   rel 1.3e-04
  first kimi block          -4.134215 vs  -4.134117   rel 1.2e-05
  a vanilla block           -0.346231 vs  -0.346258   rel 3.9e-05
  last mlp                  +1.185796 vs  +1.185661   rel 5.7e-05

Single translation unit (coder0.cpp includes tf/*.inc; -O3 -ffp-contract=off
instead of -Ofast, because one TU means one flag set and it has to be the one
the transformer is correct under).  book1000, all round-tripping:

  (defaults)                 1735
  -DTF_FP32=0                1735
  -DTF_TRAIN=1               1762
  -DTF_TRAIN=2               1765
  -DTF_TRAIN=3               1672
  -DTF_LOAD_WEIGHTS=0        2047
  -DTF_FP32=0 -DTF_TRAIN=2   1767
  g++ instead of clang++     1735

Everything is within 2 bytes of the same configuration under the old
multi-TU -Ofast build, so the tables above still stand.  Edge cases (empty,
1 byte, 20 KB of random, all 256 byte values) all round-trip.

Saving the final weights (5th argument), FX2TFWC2, the format they are read
from.  Rows keep the scale they were loaded with where it still covers them,
and an untrained embedding is written back verbatim, so a save with training
off is a byte-exact no-op; save(load(save(X))) == save(X) byte for byte, which
is what verifies the encoder against the decoder it mirrors.

Train on a 32 KB slice of book1 disjoint from book1000, save, reuse:
  book1000     shipped weights frozen 1735   trained weights frozen 1727
  32 KB slice  shipped weights frozen 11026  trained weights frozen 10973
                                             (online, one pass: 10418)
File sizes: shipped 2930652, re-saved untrained 2930603, trained 2930960.

"nul" (also NUL, nul:, /dev/null) as the weights_in argument starts from a
freshly initialized model rather than a file - the same thing a
TF_LOAD_WEIGHTS=0 build does, and byte-identical to it (book1000: 2047 both
ways).  The name is matched rather than opened: on Windows fopen("nul")
succeeds, so the loader would read an empty file and die.

Progress reporting.  The coding loop tests one mask per byte; the clock is read
on a stride that retunes itself to a fixed wall-clock spacing, so the cost does
not depend on the configuration:

  ppmd only, book1 (5.5 us/byte)   stride settles at 16384   53 clock reads total
  transformer frozen (7 ms/byte)   stride settles at 32      ~10 reads/second

Both give a half-second update cadence.  clock_gettime costs 27.5 ns here
(tsc clocksource), so the ppmd case spends about 1.5 us on timing across a
4.3 second run.  Best-of-9 interleaved runs, ppmd only on book1:
  PROGRESS=0  4.270s     PROGRESS=1  4.269s
The medians differ by 0.8% in both directions across repeats - that is machine
noise, the minima are identical.  Compressed output is byte-identical with and
without progress.

tfwc: weights file <-> BMP images.  "tfwc c file prefix" writes prefix.txt and
186 BMP v3 images (6.4 MB for the 2.9 MB file); "tfwc d prefix file" packs them
back.  Byte-identical round trip on the shipped file, on a fresh-init save and
on a re-saved file: the text file keeps the tensor order (WeightsFile::order,
recorded by both loaders) and the packer reuses weights_write.inc, which picks
the same per-tensor encoding the Python encoder did.  Shapes as in MODEL.md's
budget: the q/k/v/o projections of a block are one 192x192 RGBA image
(R=q, G=k, B=v, A=o), KDA gate pairs stack into one 8-bit image per direction,
int4 pixels are q+7 under a 15-level palette, bf16 vectors are two rows (hi/lo),
f32/i32 verbatim in 32-bit pixels, anything of 32 elements or less is hex in
the text file.  PIL decodes every image.

0_lstm_green: the ppmd+LSTM coder gets progress.inc verbatim (it has no
transformer dependency), alongside the weights load/save.  Its adaptive stride
settles at 1024 bytes with the LSTM training (~100 us/byte) and at 32768 with
PPMD alone (~1 us/byte: 28 clock reads over a 948424-byte run).  Both keep the
half-second cadence.  Best-of-9 interleaved on the fast path came out 3.582s
with progress and 3.813s without - 6% the "wrong" way, which is this machine's
noise rather than a saving, and matches what the transformer measurements above
found in the other direction.  Compressed output is byte-identical with and
without, checkpoint included.

0_lstm_green: LSTM_SAVE_Q4 stores the saved weight matrices the way the
transformer stores its own - 15 int4 levels against a per-row bf16 scale,
through tf/weights_write.inc's own quantizer, as <name>.q + <name>.scale.  The
loader takes either form tensor by tensor, so f32 and q4 checkpoints are
interchangeable between builds.  A/B/C, the AdamW variances and the config stay
f32: 90 floats each is not worth the trouble, and a variance is non-negative
with a wide dynamic range.

Checkpoint from 200000 bytes of sources, and the cost on a disjoint 80000
(fresh 18676):
  f32 + optimizer   807338   18144
  q4  + optimizer   456337   18169
  f32 weights only  404812   17932
  q4  weights only   53819   17952
So q4 costs ~0.13% and is 7.5x smaller weights-only, 15x down from the default.
Off by default: f32 is exact.

The frozen save/load identity survives quantization because quantize_row_int4
keeps a row's loaded scale when it still covers the row.  I first added a
special case for all-zero rows (lstm.output_layer has one per byte outside the
alphabet), whose scale is arbitrary and which the quantizer hands 1.0 rather
than the loaded value - then tested it by removing the special case, and the
round trip stayed byte-identical.  It cannot fire: the quantizer gives such a
row 1.0 on the first save too, so both sides already agree, and a frozen run
cannot turn a nonzero row into a zero one.  Removed again.
