# What tools/resign-drive.sh last measured about resign.py's and
# resign_group.py's offers.  The rows below were measured against the
# single-file tree and their stamp line was `# subs1.hpp <cksum>`, which
# resign.driven -- reading through buildlog.read -- has never been able to
# parse, so they have printed as "stale" ever since.  Both tools report zero
# on this tree, so there is nothing to re-measure; the format is corrected
# here for the next run that has something to say.
resign.py none of 28 reduces the count
resign_group.py none of 23 reduces the count
# sources 0 0
