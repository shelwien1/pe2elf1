# What tools/resign-drive.sh last measured about resign.py's and
# resign_group.py's offers, one row per (tool, file) -- the rule applies per
# file, so a row that does not name one is a verdict about nothing.
#
# `none of N reduces` means the driver applied each of that file's N candidates
# on its own, rebuilt with the conversion flags, and none of them lowered the
# count.  The stamp at the end is the whole tree's, so every row goes stale
# together the moment anything moves.
resign.py model.inc none of 2 reduces the count
resign.py plane_choose.inc none of 1 reduces the count
resign.py sym_code.inc none of 1 reduces the count
resign.py sym_list.inc none of 2 reduces the count
resign.py alt_p2_context.inc none of 2 reduces the count
resign.py bmp.inc none of 1 reduces the count
# sources 3281280497 502453
