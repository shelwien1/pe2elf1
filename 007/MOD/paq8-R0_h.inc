



static const int R0_D_M1 = (240+0) * (1);
static const int R0_D_M0 = (255+0) * (1);
static const int R0_E_M3 = (0+0) * (1);
static const int R0_E_M2 = (255+0) * (1);
static const int R0_E_M1 = (255+0) * (1);
static const int R0_E_M0 = (255+0) * (1);
static const int R0_CM0_CAP = (255+0) * (1);
static const int R0_CM0_SH = (2+0) * (1);
static const int R0_CM1_SH = (2+0) * (1);
static const int R0_CN1_SH = (8+0) * (1);
static const int R0_CN2_SH = (16+0) * (1);
static const int R0_CN3_MASK = (65535+0) * (1);
static const int R0_CN3_SH = (3+0) * (1);
static const int R0_CN4_MASK = (3+0) * (1);
static const int R0_CN4_TAG = (8192+0) * (1);
static const int R0_CO1_SH = (8+0) * (1);
static const int R0_CO2_MASK = (262143+0) * (1);
static const int R0_CO3_SH = (3+0) * (1);
static const int R0_CP1_SH = (8+0) * (1);
static const int R0_CP2_SH = (16+0) * (1);
static const int R0_CQ0_SH = (3+0) * (1);
static const int R0_CQ1_SH = (19+0) * (1);


static const int R0_Cm0_Volume = 1* (1<<8)* 256; // 65536
static const int R0_Cm1_Volume = 1* (1<<16)* 512; // 33554432

struct R0_T { 
#undef USE_NEW
#define USE_NEW 0


  
  /* Total bytes this module's Table() members occupy -- a constant here, since
   * Const mode has already folded every count. */
  static constexpr unsigned long long R0_Size = 0;
  void R0_Init( void ) {
  
  }
  void R0_Quit( void ) {
  }
};
