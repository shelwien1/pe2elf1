// paq8-N0.inc -- template for the generated MOD/paq8-N0_p.inc.
//
// N0 declares seven Numbers and nothing else: no Index to build and no Table()
// to allocate, so this template is empty and idx2inc.pl leaves the constants in
// paq8-N0_h.inc, where statetable.inc's generator needs them.
