// paq8-T0.inc -- template for the generated MOD/paq8-T0_p.inc.
//
// It declares only Numbers: no Index to build and no Table() to allocate, so
// this template is empty and idx2inc.pl leaves every constant in
// paq8-T0_h.inc, which is where State_table's initialiser needs them.
